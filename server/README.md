# 🚀 Workstudio x ExpressJS x Persona

This project is part of **Workstudio**, the **Visual API & Backend Development platform** that helps you build, visualize, and deploy APIs in minutes — without writing boilerplate code.

This ExpressJS setup provides a ready-to-use structure generated by Workstudio for your Persona's services.

---

## 🧩 Overview

This template gives you:
- ⚙️ Pre-configured **ExpressJS** setup
- 🗄️ Database connection support (via `src/config/database.ts`)
- 🧠 Organized folder structure for **routes**, **controllers**, and **middleware**
- 🧰 Environment-ready setup for **Docker**, **pnpm**, and **TypeScript**

Whether you generated this with Workstudio or are extending it manually, the goal is to help you **focus on logic, not setup**.

---

## 📁 Project Structure

```bash
Persona/
├── public/
│ └── assets/
│ └── .gitkeep
├── src/
│ ├── config/
│ │ └── database.ts
│ ├── routes/
│ │ └── index.ts
│ ├── controllers/
│ ├── middlewares/
│ ├── app.ts
│ └── server.ts
├── package.json
├── tsconfig.json
├── Dockerfile
└── README.md
```
---

## ⚡ Getting Started

1. Clone the repository

```bash
git clone <your-repo-url>
cd Persona
```

2. Install dependencies
```bash
pnpm install
```

3. Set up your environment
Create a .env file in the project root:

```bash
PORT=3000
DATABASE_URL=<your-database-url>
```

4. Run the development server

```bash
pnpm run dev
```

Your server should now be running on:

```bash
http://localhost:3000
```

🐳 Docker Setup
To build and run your container:

```bash
docker build -t Persona .
docker run -p 3000:3000 Persona
```

🧠 Workstudio Integration

When used with Workstudio, this project can be:
Visually modified via the Workstudio API Builder
Auto-deployed via Workstudio Deploy Linked with Workstudio Databases or external data sources
For more information, visit: https://workstudio.space

🛠️ Scripts
```bash
docker build -t workstudio-express .
docker run -p 3000:3000 workstudio-express
```

For more information, visit: https://workstudio.space

🛠️ Scripts
Command	Description

pnpm run dev	Start development server with hot reload
pnpm run build	Build for production
pnpm start	Run built server
pnpm lint	Check for linting issues

❤️ Made with Workstudio

Work smarter. Build faster.

Learn more at https://workstudio.space